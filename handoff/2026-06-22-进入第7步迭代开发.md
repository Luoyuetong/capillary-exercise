# 会话交接：进入第 7 步迭代开发

> 本文档记录**动态进度信息**，会随开发推进而更新。稳定的项目规约见 `../CLAUDE.md`。
> 最后更新：2026-06-22

---

## 一句话现状

第 7 步迭代开发**已开始**：Issue #1（项目脚手架）已完成并合并；随后做了一次**设计修正**（SQLite + 进程内 Fake，见下）。下一步 Issue #2。

---

## 已完成（第 1-6 步）

| 步骤 | 产出 | 状态 |
|------|------|------|
| 1. 工作区 + GitHub 仓库 | 仓库已建并推送 | ✅ |
| 2. 需求 | `doc/001-REQUIREMENTS.md`（FR-01~13） | ✅ |
| 3. 概要设计 | `doc/002-DESIGN.md` | ✅ |
| 4. Issue 拆解 | `doc/003-ISSUE_LIST.md` + GitHub Issues（设计修正后精简为 8 个） | ✅ |
| 5. 测试计划 | `doc/004-TEST_PLAN.md`（TC-01~23） | ✅ |
| 6. 规范底座 | `doc/005-CODING_STANDARD.md` + CI + .gitignore | ✅ |
| — 教学材料 | `doc/teaching/`（000-006 正课 + 010/020 后置） | ✅ |

所有内容已 commit 并 push 到 GitHub。

---

## 下一步

Issue #1 已完成并合并。**设计已修正**（见下方"设计修正"），Issue 精简为 8 个。

开发顺序（详见 `doc/003-ISSUE_LIST.md`）：
```
第一批：#1 项目结构 ✅、#2 数据访问(SQLite)、#3 硬件接口
第二批：#4 三个进程内 Fake
第三批：#7 PickupService
第四批：#8 PickupForm
第五批：#9 端到端测试、#10 CI
```

每个 Issue 的开发闭环见 `../CLAUDE.md`「迭代开发流程」。

---

## 设计修正（2026-06-22，聚焦教学）

第 7 步开工后，对继承自参考设计的两处选型做了调整，使教学更聚焦工程流程：

1. **数据库 Access/Jet → SQLite**：去掉强制 x86，AnyCPU + CI 可原生跑库测试。
2. **独立 Mock 程序 + TCP 客户端 → 进程内 Fake**：原 #11/12/13 + #4/5/6 改为 3 个 Fake（合并 #4），#5/6/11/12/13 关闭，13 Issue → 8 Issue。

详见 `doc/003-ISSUE_LIST.md` 开头、`doc/000-PROJECT_STARTUP.md` 附录。

---

## 接手时怎么做

1. 读 `../CLAUDE.md`（稳定规约）
2. 读本文件（当前进度）
3. 需要时查 `doc/002-DESIGN.md`（设计）和 `doc/004-TEST_PLAN.md`（测试用例）
4. 接着做 Issue #2（数据访问层）：从 main 切 `feature/issue-2-data-access`

---

## 进度更新区

> 每完成一个 Issue，在这里追加一行，保持本文件反映最新进度。

- _（尚无 Issue 完成）_

### Issue #1 ✅ 搭建项目基础结构（2026-06-22，已合并 PR #14）
- 产出：`CapillaryExercise.sln`、主项目（WinForms/net9.0-windows/AnyCPU）、测试项目（xUnit + NSubstitute）、`Models/`（CapillaryInfo、OperationLog、PickupResult）
- 验证：`dotnet build -c Release` 通过（0 警告 0 错误）；`dotnet test`（CI 过滤）退出码 0；CI 绿
- 过程：曾因 Access/Jet 锁 x86 导致 CI 缺 x86 运行时失败；改用 SQLite 后去掉 x86，CI 转绿（见"设计修正"）
- 下一步：Issue #2（数据访问层，SQLite）

### 设计修正 ✅（2026-06-22）
- 分支 `feature/design-revision-sqlite-fakes`：文档与 Issue 重构（SQLite + 进程内 Fake），不含业务代码
- 关闭 #5/6/11/12/13，#4 改为"实现 3 个 Fake"
